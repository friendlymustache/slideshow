
  	  <div class="ui center aligned segment">
  	  	<div class="ui header">My Slideshows</div>
  	  	<div class="link list">
		  {{#each session.secure.slideshows as |slideshow|}}
		  	<div class="item"> {{#link-to 'edit-slideshow' slideshow}} {{slideshow.name}} {{/link-to}} </div>
		  {{/each}}
		</div>
	  </div>

	  <form class="ui form" {{action 'makeSlideShow' on="enter"}} >
		  <div class="field">
		  	{{input value=title placeholder="Title"}}
		  </div>
		  <div class="field">
		  	<button class="ui fluid blue button" {{action 'makeSlideshow'}}>
		        Create Slideshow
		  	</button>
		  </div>

	  </form>



{{outlet}}
