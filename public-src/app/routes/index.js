/* Landing page
 *
 * Displays a button prompting user to create slideshow, at which point
 * they are redirected to the edit-slideshow route to do so.
 */
import Ember from 'ember';

export default Ember.Route.extend({
});
